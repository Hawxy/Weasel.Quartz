# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [TrustedPublishingGitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_Manual_Nuget_Push --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Manual_Nuget_Push

on: [workflow_dispatch]

permissions:
  id-token: write
  contents: read

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v4
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'NuGet login (trusted publishing)'
        id: nuget_login
        uses: NuGet/login@v1
        with:
          user: ${{ secrets.NUGET_USER }}
      - name: 'Run: NugetPush'
        run: ./build.cmd NugetPush
        env:
          NugetApiKey: ${{ steps.nuget_login.outputs.NUGET_API_KEY }}
